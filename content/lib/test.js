clear();

var path		= 'E:/05 - Commercial Projects/xJSFL/3 - development/xJSFL Komodo 7 Extension/_other/files/error.jsfl';
/*

trace(path)

var file	= xjsfl.objects.file;
file.path	= ko.uriparse.URIToPath(path);

if(file.exists)
{
	file.open('r');
	var str = file.readfile();
	trace('>' + str)
	file.close();
	trace('done')
}
*/
trace(typeof autocode)
